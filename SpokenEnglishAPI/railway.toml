[build]
builder = "NIXPACKS"
buildCommand = "dotnet publish SpokenEnglishAPI.csproj -c Release -o /app/publish"

[deploy]
startCommand = "dotnet /app/publish/SpokenEnglishAPI.dll"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
