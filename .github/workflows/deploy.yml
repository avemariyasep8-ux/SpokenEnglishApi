name: Deploy API to Railway

# Railway auto-deploys via native GitHub integration (spoken-english-api service → Settings → Source → GitHub)
# This workflow is intentionally disabled to avoid duplicate deploys.
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy handled by Railway GitHub integration"
